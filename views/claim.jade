extends layout

block content
  .page-header
    h3 Claim your Character
    p Use this interface to claim your character and upgrade your group from 'guest' to 'player'. You will need to be logged out for this to work.

  form.form-horizontal(role='form', method='POST')
    input(type='hidden', name='_csrf', value=token)
    .form-group
      label(class='col-sm-2 control-label', for='player') Character
      .col-sm-8
        input.form-control(type='text', name='player', id='player', autofocus=true)
    .form-group
      .col-sm-offset-2.col-sm-8
        button.btn.btn-default(type='submit')
          i.fa.fa-mail-forward
          | Send
